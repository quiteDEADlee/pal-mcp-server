pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-mock>=3.11.0
# Held to the 25.x line that .pre-commit-config.yaml pins, so CI, the hooks,
# and the committed formatting agree. Black 26 rewraps string literals and 23.x
# predates the docstring blank-line rule, and either one fails `black --check`
# on files nobody touched.
black>=25.1.0,<26.0.0
ruff>=0.1.0
isort>=5.12.0
python-semantic-release>=10.3.0
build>=1.0.0
